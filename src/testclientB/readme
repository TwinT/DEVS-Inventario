Este atómico lo creo para testear al cliente. Lo hice a partir del ejemplo
pulse. Lo que hace es que cada vez que recibe un mensaje por su entrada start
pasa del estado on == false al estado on == true con un ta = 1". Cuando se
vence ese tiempo se envía un mensaje por el puerto out de salida y se vuelve al
estado on == false con ta = Inf.
Según los mensajes que imprimo con cout el código avanza como debería pero no
escribe los mensajes en el archivo de salida output. Pienso que puede ser un
problema del pulse.ma que no linkea bien la salida del atómico con la salida del
del acoplado pero lo revisé varias veces y no encuentro el error.

Para ejecutarlo utilizar:
bin/cd++ -m../pulse.ma -e../pulse.ev -ooutput
